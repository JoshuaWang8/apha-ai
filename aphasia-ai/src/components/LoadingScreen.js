import React from 'react';

export const LoadingScreen = () => {
    return (
        <div className="LoadingScreen">
            {/* Add styling and animations here to transition into the app screen */}
            <h1>Loading...</h1>
        </div>
    );
}